subprojects {
    apply plugin: 'java'
 
    repositories {
        mavenCentral()
    }
}

configure(subprojects.findAll {(it.name != 'devscan') && (it.name != 'devscan-examples')}) {
    apply plugin: 'application'  
}
