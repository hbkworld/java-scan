apply plugin: 'application'
apply plugin: 'java'
 
description 'An example to use the devscan library to configre a device.'

dependencies {
    compile project(':devscan')
}
 
mainClassName = 'Sender'

